# Arch Linux 及其衍生版打包相关
- 在打包**appimage**格式时，可能由于**较新的ELF section**与Tauri 下载的 `linuxdeploy` 里自带的 `strip` 版本过旧，导致打包失败。
- 打包成 `.pkg.tar.zst`格式时，**Tauri CLI 本身不直接支持这个格式**，需要通过 Arch 官方打包工具 `makepkg` 来生成 `.pkg.tar.zst` 包。
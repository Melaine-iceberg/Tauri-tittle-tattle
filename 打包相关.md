# Arch Linux 及其衍生版打包相关

## 常见问题

### AppImage 打包问题

在打包 **AppImage** 格式时，可能由于以下原因导致打包失败：

- **较新的 ELF section** 与 Tauri 下载的 `linuxdeploy` 里自带的 `strip` 版本过旧不兼容

### PKG 打包问题

打包成 `.pkg.tar.zst` 格式时需要注意：

- **Tauri CLI 本身不直接支持这个格式**
- 需要通过 Arch 官方打包工具 `makepkg` 来生成 `.pkg.tar.zst` 包
